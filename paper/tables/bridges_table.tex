\begin{table}[H]
  \centering
  \resizebox{1.1\textwidth}{!}{%
    \pgfplotstableread[col sep=comma]{results/2025-08-29/bridges.csv}\bridgesdata
    \pgfplotstabletypeset[
      columns={Bridge,Source,Target,LossForm,Parameters,ReferenceLemma,Notes},
      columns/Bridge/.style={string type, column name=Bridge},
      columns/Source/.style={string type, column name=Source},
      columns/Target/.style={string type, column name=Target},
      columns/LossForm/.style={string type, column name=LossForm},
      columns/Parameters/.style={string type, column name=Parameters},
      columns/ReferenceLemma/.style={string type, column name=ReferenceLemma},
      columns/Notes/.style={string type, column name=Notes},
      string replace*={_}{\_},
      every head row/.style={before row=\toprule, after row=\midrule},
      every last row/.style={after row=\bottomrule}
    ]{\bridgesdata}
  }
  \caption{Bridge equivalences with explicit losses.}
  \label{tab:bridges}
\end{table}
